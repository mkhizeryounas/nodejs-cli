'use strict';

/**
 * Demo files for code style guide
 * @module Main
 */

/**
 * Add two numbers
 * @param {Number} a first number
 * @param {Number} b second number
 * @returns {Number} result of the function
 * @example
 * let x = addNumbers(2, 2);
 * @see https://github.com/mkhizeryounas
 * @todo Stuff left todo
 */
const addNumbers = (a, b) => {
  return a + b;
};

module.exports = { addNumbers };
